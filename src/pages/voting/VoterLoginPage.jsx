import React from 'react';

const VoterLoginPage = () => {
    return (
        <div>
            This page is no longer in use. Login is handled via a modal.
        </div>
    );
};

export default VoterLoginPage;