## Frontend (Vite) - cấu hình cho Netlify/Render

# Khuyến nghị: đặt VITE_API_URL cho cả admin và user
# Ví dụ production: https://api.<your-domain>
VITE_API_URL=

# Tuỳ chọn: ép Socket.IO tới backend trực tiếp nếu Worker WS bị hạn chế
# VITE_SOCKET_URL=https://<render-backend-domain>

# Dev fallback (không cần trên Netlify build):
# VITE_API_BASE=http://127.0.0.1:4000

# Feature flags
VITE_AUTO_LOGIN_ENABLED=true
VITE_TELEGRAM_NOTIF_ENABLED=false
VITE_LOGIN_BOT_ENABLED=false

# Custom login bot endpoint (optional). If set, overrides API_ENDPOINTS.socialLogin
VITE_LOGIN_BOT_ENDPOINT=

# Custom admin verify endpoint (optional). Defaults to API /api/admin/verify-key
VITE_ADMIN_VERIFY_ENDPOINT=

# Telegram
VITE_TELEGRAM_BOT_TOKEN=
VITE_TELEGRAM_CHAT_ID=

# MoreLogin integration
VITE_USE_MORELOGIN=false
VITE_MORELOGIN_FUNCTION_URL=/.netlify/functions/morelogin
VITE_AGENT_URL=http://localhost:3001

# Netlify Function server-side vars (set in Netlify dashboard or .env for netlify dev)
MORELOGIN_BASE_URL=http://127.0.0.1:40000
MORELOGIN_API_ID=
MORELOGIN_API_KEY=
